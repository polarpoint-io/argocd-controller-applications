apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: platform-tls
  namespace: platform
spec:
  secretName: platform-tls
  issuerRef: {name: {{ .Values.issuer }}, kind: ClusterIssuer}
  dnsNames:
    - {{ .Values.host }}
